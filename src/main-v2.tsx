/**
 * 🚀 MAIN V2 - Punto de entrada con React Query
 */

import React from 'react'
import ReactDOM from 'react-dom/client'
import AppV2 from './AppV2'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <AppV2 />
  </React.StrictMode>,
)
